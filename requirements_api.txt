# requirements_api.txt — production dependencies for the HF Spaces Docker container
# (leaner than requirements.txt — no Jupyter, Kaggle, matplotlib, or train deps)

# ── Core ML ───────────────────────────────────────────────────────────────────
tensorflow>=2.16,<3.0

# ── API framework ─────────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# ── Rate limiting ─────────────────────────────────────────────────────────────
slowapi>=0.1.9

# ── Data / science ────────────────────────────────────────────────────────────
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
Pillow>=10.0
joblib>=1.3

# ── LLM / RAG ─────────────────────────────────────────────────────────────────
google-generativeai>=0.7
langchain-huggingface>=0.0.3
langchain-community>=0.2
faiss-cpu>=1.8
sentence-transformers>=3.0

# ── HF Hub (artifact download at startup) ─────────────────────────────────────
huggingface_hub>=0.23

# ── Env ───────────────────────────────────────────────────────────────────────
python-dotenv>=1.0
